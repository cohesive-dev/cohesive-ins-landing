import {validField,type Answers} from './contractor-experiment';
import {TRADES} from './contractor-trades';
import {PRIMARY_PCT,REVENUE,EMPLOYEES,PAYROLL,USES_SUBCONTRACTORS,SUBCONTRACTOR_COSTS,STRUCTURE,CURRENT_GL,CURRENT_PREMIUM} from './contractor-full-options';
export const FULL_LABELS:Record<string,string>={email:'Email address',phone:'Phone number',fullName:'Full name',legalName:'Legal business name',address:'Business address',trade:'Primary trade',tradeRawText:'Primary trade search text',otherTradesRawText:'Other trades search text',otherTrades:'Other trades',primaryPct:'Primary trade % of work',otherTradeDescription:'Other trade description',revenue:'Annual revenue',employees:'W2 employees',payroll:'Annual W2 payroll',usesSubcontractors:'Uses subcontractors',subcontractorCosts:'Annual subcontractor costs',structure:'Business structure',yearStarted:'Year started',currentGl:'Current GL / renewal',currentPremium:'Current annual GL premium'};
export function fullScreens(a:Answers){return [['email','phone','fullName'],['legalName','address'],['trade','otherTrades','primaryPct',...((a.trade==='Other trade'||a.otherTrades?.split(', ').includes('Other trade'))?['otherTradeDescription']:[])],['revenue','employees',...(a.employees&&!a.employees.startsWith('0')?['payroll']:[])],['usesSubcontractors',...(a.usesSubcontractors==='Yes'?['subcontractorCosts']:[])],['structure','yearStarted'],['currentGl','currentPremium']];}
const choices:Record<string,{value:string}[]>={trade:TRADES,primaryPct:PRIMARY_PCT,revenue:REVENUE,employees:EMPLOYEES,payroll:PAYROLL,usesSubcontractors:USES_SUBCONTRACTORS,subcontractorCosts:SUBCONTRACTOR_COSTS,structure:STRUCTURE,currentGl:CURRENT_GL,currentPremium:CURRENT_PREMIUM};
export function validFullField(k:string,v:string|undefined){if(['structure','yearStarted','currentGl','currentPremium'].includes(k)&&!v?.trim())return true;if(k==='otherTrades')return !!v&&(v==='None'||v.split(', ').every(t=>TRADES.some(x=>x.value===t)));if(choices[k])return choices[k].some(x=>x.value===v);if(k==='yearStarted')return true;if(['fullName','legalName','address','otherTradeDescription'].includes(k))return !!v?.trim();return validField(k,k==='email'?v?.trim():v);}
export function normalizeFull(a:Answers,k:string,v:string){const next={...a,[k]:v};if(k==='trade'&&next.otherTrades&&next.otherTrades!=='None'){next.otherTrades=next.otherTrades.split(', ').filter(t=>t!==v).join(', ');}if(k==='employees'&&v.startsWith('0'))delete next.payroll;if(k==='usesSubcontractors'&&v==='No')delete next.subcontractorCosts;if(next.trade!=='Other trade'&&!next.otherTrades?.split(', ').includes('Other trade'))delete next.otherTradeDescription;return next;}
